apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: small-project
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: small-project
                port:
                  number: 8000