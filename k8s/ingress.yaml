apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-test
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: demo-test
                port:
                  number: 8000